syntax = "proto2";

package protobuf;

message Playlist {
  required string name = 1;

  message Audio {
        required string path = 1;
  }

  repeated Audio audio = 2;
}

message Playlists {
    repeated Playlist playlist = 1;
}
